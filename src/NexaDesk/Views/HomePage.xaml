<Page
    x:Class="NexaDesk.Views.HomePage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Padding="32,24,32,40" Spacing="22">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="6">
                    <TextBlock Text="Windows 动作中枢" FontSize="30" FontWeight="SemiBold" />
                    <TextBlock
                        Text="搜索应用、执行系统动作，并把常用步骤组合为工作流。"
                        Opacity="0.72" FontSize="14" />
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding OpenPaletteCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE721;" />
                        <TextBlock Text="打开命令面板" />
                    </StackPanel>
                </Button>
            </Grid>

            <Border Padding="18" CornerRadius="14" Background="{ThemeResource NexaCardBrush}">
                <StackPanel Spacing="12">
                    <TextBlock Text="快捷动作" FontSize="18" FontWeight="SemiBold" />
                    <ItemsControl ItemsSource="{Binding Favorites}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid MaximumRowsOrColumns="2" Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button
                                    Width="290" Height="78" Margin="0,0,10,10"
                                    HorizontalContentAlignment="Stretch"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Command="{Binding DataContext.RunActionCommand, ElementName=Root}"
                                    CommandParameter="{Binding}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="42" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon Glyph="{Binding IconGlyph}" FontSize="22" />
                                        <StackPanel Grid.Column="1" Spacing="3">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Description}" Opacity="0.68"
                                                       FontSize="12" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Padding="18" CornerRadius="14" Background="{ThemeResource NexaCardBrush}">
                <StackPanel Spacing="12">
                    <TextBlock Text="最近执行" FontSize="18" FontWeight="SemiBold" />
                    <ListView ItemsSource="{Binding RecentHistory}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="4,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="170" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding ActionName}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Message}" Opacity="0.65" FontSize="12" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding StatusText}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{Binding ExecutedAt}" Opacity="0.65"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <TextBlock Text="{Binding StatusMessage}" Opacity="0.7" />
        </StackPanel>
    </ScrollViewer>
</Page>
